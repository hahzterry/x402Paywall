# Basic Trial for x402

This is a middle layer that coordinates between the client and server.
When the webpage is loaded, the server throws a 402 - Payment Required error to the client, when the client pays the hard coded 1 cent in USDC to the server wallet, the server gives the client the premium content behind the paywall
