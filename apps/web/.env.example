# Server-only thirdweb credentials (never expose the secret key to the browser).
# Get these from https://thirdweb.com/create-api-key and your project dashboard.
THIRDWEB_SECRET_KEY=
THIRDWEB_SERVER_WALLET_ADDRESS=
